import CreateProposalClient from "./CreateProposalClient";

export const metadata = {
  title: "Create Proposal | Kastj",
  description: "Launch your conditional crowdfunding campaign on Kaspa.",
};

export default function CreateProposalPage() {
  return <CreateProposalClient />;
}
